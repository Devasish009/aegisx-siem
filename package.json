{
  "name": "aegisx",
  "version": "1.0.0",
  "private": true,
  "description": "AegisX — Enterprise SIEM & Intrusion Detection System",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --include=dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/src/server.js"
  }
}
